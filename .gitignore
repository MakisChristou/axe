/target
.env
cache/
output/
.claude/
axe-load-test-logs/
bench-results/

# Local-only baseline / comparison helpers used during the polish refactor.
# Kept on disk for re-use, not pushed.
scripts/run-baseline-matrix.sh
scripts/compare-load-test-baseline.sh
scripts/plot_load_tests.py

# Key material belongs outside the repo entirely (see AGENTS.md 6). These
# patterns are a backstop, not the policy.
*.mnemonic
*.seed
axelar1*.txt

# Nix build output symlink
/result
