/target
.env
cache/
output/
.claude/
axe-load-test-logs/
bench-results/

# Local-only baseline / comparison helpers used during the polish refactor.
# Kept on disk for re-use, not pushed.
scripts/run-baseline-matrix.sh
scripts/compare-load-test-baseline.sh
scripts/plot_load_tests.py

# Key material belongs outside the repo entirely (see AGENTS.md 6). These
# patterns are a backstop, not the policy.
*.mnemonic
*.seed
axelar1*.txt

# Nix build output symlink
/result

# Personal agent configuration, deliberately not shared: AGENTS.local.md and
# its CLAUDE.local.md pointer, plus the docs/agents/ files they reference.
# Preferences only — key material still belongs outside the repo entirely, see
# the note above and AGENTS.md 6.
*.local.md
docs/agents/
